---
// Preserve campaign attribution when old links reach the retired jobs overview.
export const prerender = false;
return Astro.redirect(`/jobs/logopaedin-sprachtherapeut-duisburg/${Astro.url.search}`, 308);
---
